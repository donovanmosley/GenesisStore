import React, { Component } from 'react'


const ProductContext = React.createContext();
//Every React.reateContext object comes with 2 components:
//Provider - Provides all information for the application
//Consmer - Uses the information from the consumer

class ProductProvider extends Component {
    render() {
        return (
            <div>
                
            </div>
        )
    }
}
